# include <stdio.h>

int main()
{
    int a, b;
    scanf("%d%d", &a, &b);
    int c = a+b;
    if(a == b)
        while(1);
    if(a > 1500 && b > 1500)
    {
        int* ptr = NULL;
        int c = *ptr;
    }
    printf("hello from C program %d\n", c);
    return 0;
}